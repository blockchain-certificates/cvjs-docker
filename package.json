{
  "name": "@blockcerts/cvjs-docker",
  "version": "0.0.0-dev",
  "description": "A docker container for Cert-Verifier-JS node server",
  "main": "index.js",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:docker": "vitest -c vitest.docker.config.ts --testTimeout 30000",
    "build": "docker build . -t blockcerts/cert-verifier-js:$(cat .version)",
    "dev": "npx tsc src/server.ts",
    "start": "docker run -p 9000:4000 -d blockcerts/cert-verifier-js:$(cat .version)",
    "start:dev": "docker run -p 9000:4000 -v \"$(pwd)/../cert-verifier-js\":/usr/src/app/node_modules/@blockcerts/cert-verifier-js blockcerts/cert-verifier-js:$(cat .version)",
    "semantic-release": "npx --package semantic-release --package @semantic-release/commit-analyzer --package @semantic-release/release-notes-generator --package @semantic-release/github --package @semantic-release/exec semantic-release"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/blockchain-certificates/cvjs-docker.git"
  },
  "keywords": [
    "blockcerts",
    "verifiable",
    "credentials",
    "verifier",
    "docker",
    "server",
    "node"
  ],
  "author": "Julien Fraichot",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/blockchain-certificates/cvjs-docker/issues"
  },
  "homepage": "https://github.com/blockchain-certificates/cvjs-docker#readme",
  "dependencies": {
    "@blockcerts/cert-verifier-js": "^6.35.2",
    "body-parser": "^1.20.2",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.5",
    "node-fetch-commonjs": "^3.2.4",
    "tslib": "^2.6.2",
    "typescript": "^5.8.3",
    "vc-api-verifier-test-suite": "github:blockchain-certificates/vc-api-verifier-test-suite#blockcerts",
    "vitest": "^4.1.0"
  },
  "overrides": {
    "picomatch": "4.0.3"
  }
}
